#include<stdlib.h>
#include<stdio.h>
/*La funzione prende come input un puntatore a FILE già aperto in modalità tradotta (testo) e un intero 
senza segno n e deve scrivere sul file i valori della tavola pitagorica arrivando fino a n, separati da tabulazioni. Ogni riga deve terminare con un carattere a capo. Ad*/
void stampa_tavola_pitagorica(FILE *f, unsigned int n) {
	for()
	fscanf(f,//cosa scrivere,preso da quali variabili)

}